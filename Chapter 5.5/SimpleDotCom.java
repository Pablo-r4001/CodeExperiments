import java.util.arrayList

class DotCom
{
	pribate ArrayList<String>  locationCells;
	
	public void setLocationCells (setLocationCells(ArrayList<String> loc)
	{
		locationCells = loc;
	}
	
	public String checkYourself(String stringGuess)
	{
		String result = "miss";
		
			int index = locationCells.indexOf(userInput);
			
			if  (index >= 0);
			{
				if (locationCells.isEmpty())
				{
					result = "kill";
				}
					result = "hit"
				}
			}
		System.out.println(result);
			return result;
	}
}